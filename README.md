# My personal Portfolio

### Run app locally

1. once the repo has been cloned, cd into `PORTFOLIO-CLIENT` folder and run `npm install` (you may have to run `npm install --force` if you get an error)
2. run `npm start` to start the app at **localhost:3000**
